export const metadata = {
  title: "Bill Management",
  description: "Manage bills for the mess management system",
};

import Client from "./client";

export default function BillsPage() {
  return <Client />;
}