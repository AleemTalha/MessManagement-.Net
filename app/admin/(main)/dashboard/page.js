export const metadata = {
    title: "Admin Dashboard",
    description: "Admin Dashboard for Mess Management System"
}

import Client from "./client";

export default function Page() {
    return <Client />;
}
