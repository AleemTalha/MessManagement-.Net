export const metadata = {
  title: "User Management",
  description: "Manage users for the mess management system",
};

import Client from "./client";

export default function UsersPage() {
  return <Client />;
}
