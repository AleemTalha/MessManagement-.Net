export const metadata = {
  title: "Attendance Management",
  description: "Manage user attendance for the mess management system",
};

import Client from "./client";

export default function AttendancePage() {
  return <Client />;
}
