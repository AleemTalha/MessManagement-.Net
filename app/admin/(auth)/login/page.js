export const metadata = {
    title : "Admin Login",
    description : "Login to the Admin Panel of the Mess Management System"
}

import Client from "./client";

export default function LoginPage() {
    return <Client />;
}