export const metadata = {
    title : "User Login",
    description : "Login to your Mess Management account"
}

import Client from "./client";

export default function LoginPage() {
    return <Client />;
}